repositories:
  - name: jenkins
    url: https://charts.jenkins.io
releases:
  - name: jenkins-infra
    chart: jenkins/jenkins
    version: 3.8.8
    namespace: jenkins-infra
    values:
    - ../config/jenkins-infra.yaml

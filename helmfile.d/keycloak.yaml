repositories:
  - name: codecentric
    url: https://codecentric.github.io/helm-charts
  - name: jenkins-infra
    url: https://jenkins-infra.github.io/public-charts
releases:
  - # https://admin.accounts.jenkins.io/auth/admin
    name: keycloak # Admin
    namespace: keycloak
    chart: codecentric/keycloak
    version: "15.1.0"
    wait: true
    timeout: 600
    atomic: true
    values:
      - "../config/publick8s/keycloak.yaml"
    secrets:
      - "../secrets/config/keycloak/secrets.yaml"
  - # https://admin.accounts.jenkins.io/auth/realms/jenkins/account
    name: keycloak-public # User
    namespace: keycloak
    chart: jenkins-infra/keycloak
    version: 0.1.1
    wait: true
    timeout: 150
    atomic: true
    values:
      - "../config/publick8s/keycloak-public.yaml"

releases:
    - name: ldap
      namespace: ldap
      chart: jenkins-infra/ldap
      version: 0.0.2
      wait: true
      timeout: 600
      values:
        - "../config/default/ldap.yaml"
      secrets:
        - "../secrets/config/ldap/secrets.yaml"

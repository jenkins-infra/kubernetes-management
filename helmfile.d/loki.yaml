repositories:
  - name: grafana
    url: https://grafana.github.io/helm-charts
releases:
  - name: loki
    namespace: loki
    chart: grafana/loki
    version: 2.8.1
    wait: true
    timeout: 300
    force: false
    atomic: true
    values:
      - "../config/loki.yaml"

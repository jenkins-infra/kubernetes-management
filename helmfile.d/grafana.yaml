releases:
  - name: grafana
    namespace: grafana
    chart: grafana/grafana
    version: "6.17.5"
    wait: true
    timeout: 300
    atomic: true
    values:
      - "../config/default/grafana.yaml"
    secrets:
      - "../secrets/config/grafana/secrets.yaml"

resources:
  limits:
    cpu: 200m
    memory: 1024Mi
  requests:
    cpu: 200m
    memory: 1024Mi
persistence:
  enabled: true
  accessModes:
    - ReadWriteOnce
  size: 500Gi
  storageClassName: managed-csi

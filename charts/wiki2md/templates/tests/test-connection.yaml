apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "wiki2md.fullname" . }}-test-connection"
  labels:
{{ include "wiki2md.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "wiki2md.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
